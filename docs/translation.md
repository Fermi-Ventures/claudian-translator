# Translation of C:/Users/robhu/AppData/Local/Temp/claude/E--repos-fermi-quality/aa06f19e-7751-40a1-9407-6ec53ba6c620/scratchpad/cto-page.md

paragraphs: 10 flagged · 10 restructured · 0 refused · 260s — then 70 sentences · 10 flagged · 6 rewritten and applied · 1 proposed only (refused by the gate) · 3 kept · 0 unparsed · find 253s · rewrite 49s · check 5s · 8 in flight · 108 calls, 0 retries, 0 rate-limited · cap 40/min

## Paragraph breaks (phase 0)

11 paragraphs became 11; paragraph-length cv 0.28 → 0.57. Words unchanged (checked). Merged the 'What did it cost' paragraph with the 'Assume 400 words a page' paragraph (single rule cost flowing into scaled cost — one thought), and split the 'dollars are small' paragraph before 'The human reviewer remains the last step' (cost/value discussion vs. the reviewer's continuing role — a subject change), leaving the already-short table paragraphs alone.

## Sentences (phase 2)

| shape | before | after | note |
|---|---|---|---|
| an abstraction in a person's role | One return objected to a word that only the drafting conversation had defined. | In one round of feedback, the reviewer objected to a word that only the drafting conversation had defined. | Replaced 'return' (standing in for the reviewer) with 'the reviewer', who is the one performing the objecting. · target check: same |
| ordinary words with insider meaning | The author of this page averaged 18 semicolons per letter. | (kept) | The author must define what a 'letter' is (a document? a chapter? a standard?) and state whether 'this page' means the review-lane document itself or one of the specific texts just analyzed (the shortest returned text, the longer standard, or the two process chapters), since without that the sentence can't be tied to a number a reader could check. |
| long sentence | On the shortest standard, the checks found ten defects between the returned version and the current one: four defects of form, two ambiguities that a busy reader would resolve the wrong way, two insider phrases or reasons placed inside a rule, one candidate contradiction, and one clause that failed to reach the reader it was written for. | On the shortest standard, the checks found ten defects between the returned version and the current one. Four were defects of form. The remaining six were two ambiguities that a busy reader would resolve the wrong way, two insider phrases or reasons placed inside a rule, one candidate contradiction, and one clause that failed to reach the reader it was written for. | Split the 57-word sentence into three shorter sentences, isolating the total, the form-defect count, and the remaining category breakdown, without altering the counts or categories given. · target check: same |
| ordinary words with insider meaning | While the regex script, which had flagged four of five, now flags none of its fourteen sentences, the readability alerts fell from three to none. | While the regex script — which had originally flagged four of the five defect categories listed above — now flags none of the document's fourteen sentences, the readability alerts fell from three to none. | Made explicit that 'five' refers to the five defect categories enumerated in the preceding sentence (form, ambiguity, insider phrases, contradiction, clause reach), and changed 'its fourteen sentences' to 'the document's fourteen sentences' since 'its' otherwise reads as attaching to the regex script. · target check: same |
| ordinary words with insider meaning | The reach test, which asks a model reader in a specific role whether the rule binds it, rose from four personas reached in six to eight in eight, though whether the reviewer accepts the current text is not yet known since he has not read it. | (proposed, not applied) The reach test asks a model reader playing a specific role whether the rule binds it. Before the rewrite, it reached four of the six personas tested; after, it reached all eight of eight. Whether the reviewer accepts the current text is not yet known. *(reason for the note: the reviewer has not read it)* | REFUSED: negation count changed: 2 → 1 |
| coined noun | The model checks cost about eight cents per iteration on a 300-word rule: the insider-prose finder cost half a cent, the two-reader diff three cents, the strict probe one cent, and the reach personas three cents. | (kept) | These four names (insider-prose finder, two-reader diff, strict probe, reach personas) are not defined anywhere in the given paragraph or sentence, so no plain-language substitute can be written without inventing what each check does; the author must either define each on first use earlier in the document or add a short parenthetical gloss here. |
| an abstraction in a person's role | The figures below use our measured costs and state the assumptions. | Below, we calculate the figures from our measured costs and state our assumptions. | Changed the subject from 'the figures' to the author ('we'), since figures cannot use costs or state assumptions themselves. · target check: same |
| coined noun | The model checks (the finder, the diff and the probe) read meaning and new phrases. | (kept) | The finder, the diff and the probe are named as the two model checks, but neither this sentence nor the paragraph says what each one individually inspects; the author must supply a short phrase for what each of the three actually does before the names can be de-jargoned. |
| long sentence | Only the model checks caught four: the clause that a weak reader took as a condition, the two-item list that a weak reader took as closed, the coined phrase that was not yet on any list, and the ordinary words that carried an insider meaning. | Only the model checks caught four things: the clause that a weak reader took as a condition, and the two-item list that a weak reader took as closed. They also caught the coined phrase that was not yet on any list, and the ordinary words that carried an insider meaning. | Split the 45-word sentence into two shorter sentences, preserving all four listed items without adding or removing meaning. · target check: same |
| ordinary words with insider meaning | It carries the prerequisites, the Vale config, the three prompts, the calibration step, the loop, the costs, the five writing rules that make text pass and the rewrites above. | It carries the prerequisites, the Vale config, the three prompts, the calibration step, the loop, the costs, the five writing rules that keep a document from being flagged by the linter and the model checks, and the rewrites above. | Replaced 'make text pass' with what passing means in this lane: not being flagged by the linter or the two model checks. · target check: same |

## Paragraphs (phase 1: structure)

| ¶ | counters | before | after | note |
|---|---|---|---|---|
| 1 | TRIAD x2 | Standards, specs and agent briefs written inside a long AI-assisted conversation come out readable only to the people who were in that conversation. This page describes a review lane, a fixed sequence of checks that a document passes through before a human reviewer reads it. The lane catches the problem first. It uses a free prose linter for form and known phrases, and two cheap model calls for meaning. One loop joins them: every phrase the models find is added to the linter's list, so the free check grows. The page shows how the lane runs, what it found in its first week, what it cost, and it ends with a playbook any team can hand to an agent. | Standards, specs and agent briefs written inside a long AI-assisted conversation come out readable only to the people who were in that conversation. This page describes a review lane, a fixed sequence of checks that a document passes through before a human reviewer reads it. The lane catches the problem first. It uses a free prose linter for form and known phrases, and two cheap model calls for meaning. One loop joins them: every phrase the models find is added to the linter's list, so the free check grows. The page shows how the lane runs, what it found in its first week, and what it cost. It ends with a playbook any team can hand to an agent. | Both triads (the opening document list and the how/found/cost list) are real, distinct items, so their content stays untouched; the fix instead splits the run-on tail off the second triad into its own short sentence, removing the ', and it ends with...' join and giving the paragraph a genuine short/long rhythm break instead of a four-part list. · still flagged: UNIFORM, TRIAD | Standards, specs and agent briefs written inside a long AI-assisted conversation come out readable only to the people who were in that conversation. This page describes a review lane, a fixed sequence of checks that a document passes through before a human reviewer reads it. The lane catches the problem first. It uses a free prose linter for form and known phrases, and two cheap model calls for meaning. One loop joins them: every phrase the models find is added to the linter's list, so the free check grows. The page shows how the lane runs, what it found in its first week, what it cost, and it ends with a playbook any team can hand to an agent. |
| 2 | UNIFORM (cv 0.31, lengths 10/18/16/7/8/15/14); TRIAD x1 | The lane has two halves and a loop between them. The linter counts sentence length, readability grade and semicolons, and it matches every phrase it has been taught. The models read for meaning and discover the phrases the linter has not been taught yet. The human reviewer makes the final call. No step in the lane blocks a document. Each flag is a note that the author either rewrites or defends on the record. Every phrase a model or the reviewer names goes back into the linter’s list. | The lane has two halves and a loop between them. The linter counts sentence length, readability grade and semicolons, matching every phrase it has been taught, while the models read for meaning and discover the phrases the linter has not been taught yet. The human reviewer makes the final call. No step in the lane blocks a document. Each flag is a note that the author either rewrites or defends on the record. Every phrase a model or the reviewer names goes back into the linter’s list. | Merged the linter and model sentences into one longer, flowing sentence with 'while' (no semicolon/dash/', and') to break the uniform sentence-length pattern, leaving the two short sentences short; the three-item list (sentence length, readability grade, semicolons) is a genuine list, not padding, so it stayed untouched. · still flagged: TRIAD | The lane has two halves and a loop between them. The linter counts sentence length, readability grade and semicolons, and it matches every phrase it has been taught. The models read for meaning and discover the phrases the linter has not been taught yet. The human reviewer makes the final call. No step in the lane blocks a document. Each flag is a note that the author either rewrites or defends on the record. Every phrase a model or the reviewer names goes back into the linter’s list. |
| 3 | UNIFORM (cv 0.18, lengths 14/10/16/13/10/12/10) | Two engineering standards were drafted by AI agents during a week of design conversation. The human reviewer returned them five times in 48 hours. Each time the meaning was intact, and each time the reviewer could not follow the text. One return objected to a word that only the drafting conversation had defined. One objected to a sentence that made two separate demands. One objected to a reason placed where a rule should have been. After thirteen revisions, both standards were still awaiting his approval. | Two engineering standards, drafted by AI agents during a week of design conversation, came back from the human reviewer five times in 48 hours. Each time the meaning was intact, and each time the reviewer could not follow the text. One return objected to a word that only the drafting conversation had defined. One objected to a sentence that made two separate demands. One objected to a reason placed where a rule should have been. After thirteen revisions, both standards were still awaiting his approval. | Folded the first two same-length sentences into one longer sentence via an appositive (no and/semicolon/dash join) to break the uniform rhythm; left the genuine three-item list of objections untouched since each item names a distinct complaint. · still flagged: UNIFORM | Two engineering standards were drafted by AI agents during a week of design conversation. The human reviewer returned them five times in 48 hours. Each time the meaning was intact, and each time the reviewer could not follow the text. One return objected to a word that only the drafting conversation had defined. One objected to a sentence that made two separate demands. One objected to a reason placed where a rule should have been. After thirteen revisions, both standards were still awaiting his approval. |
| 4 | PIVOT x1 | How much defect the originals carried? The shortest of the returned texts had five sentences. A regex script that counts sentence length and clause markers flagged four of them. Vale scored it at Flesch-Kincaid grade 16 with four sentences over thirty words. A longer standard of 24 sentences had six flagged. Two process chapters that went through the lane next carried 118 semicolons and 43 sentences over thirty words in 5,300 words. The author of this page averaged 18 semicolons per letter. The defect was not a few bad sentences but the way the text was written. | How much defect the originals carried? The shortest of the returned texts had five sentences. A regex script that counts sentence length and clause markers flagged four of them. Vale scored it at Flesch-Kincaid grade 16 with four sentences over thirty words. A longer standard of 24 sentences had six flagged. Two process chapters that went through the lane next carried 118 semicolons and 43 sentences over thirty words in 5,300 words. The author of this page averaged 18 semicolons per letter. The defect ran through the way the text was written, not a handful of bad sentences. | Reordered the closing sentence from the leading 'not X but Y' pivot to a 'Y, not X' construction, keeping every claim and roughly the same length. · counters clean | How much defect the originals carried? The shortest of the returned texts had five sentences. A regex script that counts sentence length and clause markers flagged four of them. Vale scored it at Flesch-Kincaid grade 16 with four sentences over thirty words. A longer standard of 24 sentences had six flagged. Two process chapters that went through the lane next carried 118 semicolons and 43 sentences over thirty words in 5,300 words. The author of this page averaged 18 semicolons per letter. The defect was not a few bad sentences but the way the text was written. |
| 5 | TRIAD x1; PIVOT x1 | How much the lane resolved? On the shortest standard, the checks found ten defects between the returned version and the current one: four defects of form, two ambiguities that a busy reader would resolve the wrong way, two insider phrases or reasons placed inside a rule, one candidate contradiction, and one clause that failed to reach the reader it was written for. All ten are resolved in the current text. The regex script now flags none of its fourteen sentences where it had flagged four of five. The readability alerts fell from three to none. The reach test, which asks a model reader in a specific role whether the rule binds it, rose from four personas reached in six to eight in eight. Whether the reviewer accepts the current text is not yet known, because he has not read it. | How much the lane resolved? On the shortest standard, the checks found ten defects between the returned version and the current one: four defects of form, two ambiguities that a busy reader would resolve the wrong way, two insider phrases or reasons placed inside a rule, one candidate contradiction, and one clause that failed to reach the reader it was written for. All ten are resolved in the current text. While the regex script, which had flagged four of five, now flags none of its fourteen sentences, the readability alerts fell from three to none. The reach test, which asks a model reader in a specific role whether the rule binds it, rose from four personas reached in six to eight in eight, though whether the reviewer accepts the current text is not yet known since he has not read it. | Broke the three-in-a-row parallel metric sentences (regex/readability/reach) by folding the regex and readability claims into one subordinate-clause sentence, and removed the setup-reversal pivot closer by folding it into the preceding reach-test sentence with a 'though' clause. · still flagged: TRIAD | How much the lane resolved? On the shortest standard, the checks found ten defects between the returned version and the current one: four defects of form, two ambiguities that a busy reader would resolve the wrong way, two insider phrases or reasons placed inside a rule, one candidate contradiction, and one clause that failed to reach the reader it was written for. All ten are resolved in the current text. The regex script now flags none of its fourteen sentences where it had flagged four of five. The readability alerts fell from three to none. The reach test, which asks a model reader in a specific role whether the rule binds it, rose from four personas reached in six to eight in eight. Whether the reviewer accepts the current text is not yet known, because he has not read it. |
| 6 | TRIAD x3 | What did it cost, and who paid? The deterministic checks cost nothing and ran in milliseconds. They found about four of the ten defects, all of them defects of form, and two of those four had passed every model reader. The model checks cost about eight cents per iteration on a 300-word rule: the insider-prose finder cost half a cent, the two-reader diff three cents, the strict probe one cent, and the reach personas three cents. Four iterations came to about thirty cents and twenty minutes of machine time when the calls ran one after another, or about five minutes when they ran eight at a time. The model checks found the other six defects, the ones about meaning. The human cost was the largest. One person read the flags for about ten minutes per iteration, and the rule's owner rewrote the text. Across four iterations, the human cost came to about an hour of two people's time for one rule. Assume 400 words a page: 40,000 words, about 2,700 sentences. The per-sentence and per-page calls are independent of each other, so they run in parallel. The machine times below give both figures, one call at a time and eight at a time. The API rate limit is the only ceiling. Our measured rates come from rule text, not end-user prose. End-user documentation written by Claude will carry more of the generic chatbot register, which the deterministic lane catches well, and fewer coined phrases. So the deterministic share of catches should rise. The figures below use our measured costs and state the assumptions. | What did it cost, and who paid? The deterministic checks cost nothing and ran in milliseconds. They found about four of the ten defects, all of them defects of form, and two of those four had passed every model reader. The model checks cost about eight cents per iteration on a 300-word rule: the insider-prose finder cost half a cent, the two-reader diff three cents, the strict probe one cent, and the reach personas three cents. Four iterations came to about thirty cents and twenty minutes of machine time when the calls ran one after another, or about five minutes when they ran eight at a time. The model checks found the other six defects, the ones about meaning. The human cost was the largest. One person read the flags for about ten minutes per iteration, and the rule's owner rewrote the text. Across four iterations, the human cost came to about an hour of two people's time for one rule. Assume 400 words a page: 40,000 words, about 2,700 sentences. The per-sentence and per-page calls are independent of each other, so they run in parallel. The machine times below give both figures, one call at a time and eight at a time. The API rate limit is the only ceiling. Our measured rates come from rule text, not end-user prose. End-user documentation written by Claude will carry more of the generic chatbot register, which the deterministic lane catches well. It will carry fewer coined phrases. So the deterministic share of catches should rise. The figures below use our measured costs and state the assumptions. | Split the 'chatbot register / coined phrases' sentence in two, since its middle clause was a relative clause riding on the first item rather than a true third parallel item, so the rule-of-three cadence there was an artifact; left the other two flagged triads (the defects-found sentence and the 400-words/40,000-words/2,700-sentences list) untouched because each is a genuine three-item set with no item restating another. · still flagged: TRIAD | What did it cost, and who paid? The deterministic checks cost nothing and ran in milliseconds. They found about four of the ten defects, all of them defects of form, and two of those four had passed every model reader. The model checks cost about eight cents per iteration on a 300-word rule: the insider-prose finder cost half a cent, the two-reader diff three cents, the strict probe one cent, and the reach personas three cents. Four iterations came to about thirty cents and twenty minutes of machine time when the calls ran one after another, or about five minutes when they ran eight at a time. The model checks found the other six defects, the ones about meaning. The human cost was the largest. One person read the flags for about ten minutes per iteration, and the rule's owner rewrote the text. Across four iterations, the human cost came to about an hour of two people's time for one rule. Assume 400 words a page: 40,000 words, about 2,700 sentences. The per-sentence and per-page calls are independent of each other, so they run in parallel. The machine times below give both figures, one call at a time and eight at a time. The API rate limit is the only ceiling. Our measured rates come from rule text, not end-user prose. End-user documentation written by Claude will carry more of the generic chatbot register, which the deterministic lane catches well, and fewer coined phrases. So the deterministic share of catches should rise. The figures below use our measured costs and state the assumptions. |
| 7 | TRIAD x1 | The dollars are small and almost entirely model spend. The machine time is short once the calls run in parallel. The hours are the real cost, and they are human hours spent reading the flags (the notes each check produces) and rewriting. The deterministic lane earns its place twice. It spends none of the dollars. It also produces the flags a human can act on fastest, since a sentence count and a phrase match need no judgment. The model lane earns its place by finding the defects that no list can hold, at a price below one lunch for a hundred pages. | The dollars are small and almost entirely model spend. Once the calls run in parallel, the machine time is short. The hours are the real cost: human hours spent reading the flags (the notes each check produces) and rewriting. The deterministic lane earns its place twice. It spends none of the dollars. It also produces the flags a human can act on fastest, since a sentence count and a phrase match need no judgment. The model lane earns its place by finding the defects that no list can hold, at a price below one lunch for a hundred pages. | Broke the three-in-a-row "The X is/are Y" tricolon by inverting the machine-time sentence and recasting the hours sentence's ", and they are" clause as a colon, without joining any sentences. · still flagged: TRIAD | The dollars are small and almost entirely model spend. The machine time is short once the calls run in parallel. The hours are the real cost, and they are human hours spent reading the flags (the notes each check produces) and rewriting. The deterministic lane earns its place twice. It spends none of the dollars. It also produces the flags a human can act on fastest, since a sentence count and a phrase match need no judgment. The model lane earns its place by finding the defects that no list can hold, at a price below one lunch for a hundred pages. |
| 9 | TRIAD x1 | Every left-hand sentence was sent back by the reviewer or flagged by the lane. Every right-hand sentence is the text that replaced it. Four shapes recur: a metaphor that carries a definition, a coined noun used as if it had been defined, a reason welded into the rule, and an abstraction put in a person's role. | Every left-hand sentence was sent back by the reviewer or flagged by the lane. Every right-hand sentence replaced it. The shapes that recur are a metaphor that carries a definition, a coined noun used as if it had been defined, a reason welded into the rule, and an abstraction put in a person's role. | Broke the matched-length pair of 'Every left-hand/right-hand sentence' clauses by trimming the second to a short stop, and dropped the 'Four shapes recur:' count-announcing signpost, folding its content into a plain lead-in since the four-item list already shows the structure. · still flagged: TRIAD | Every left-hand sentence was sent back by the reviewer or flagged by the lane. Every right-hand sentence is the text that replaced it. Four shapes recur: a metaphor that carries a definition, a coined noun used as if it had been defined, a reason welded into the rule, and an abstraction put in a person's role. |
| 10 | TRIAD x1 | The deterministic checks (Vale and the phrase lists) read form and known phrases. The model checks (the finder, the diff and the probe) read meaning and new phrases. In the table above, only the deterministic checks caught three things: the 57-word sentence that a fluent reader repairs without noticing, the reason placed inside a rule that two reviewers had cleared, and the 29 semicolons in one letter. Only the model checks caught four: the clause that a weak reader took as a condition, the two-item list that a weak reader took as closed, the coined phrase that was not yet on any list, and the ordinary words that carried an insider meaning. | The deterministic checks (Vale and the phrase lists) read form and known phrases. The model checks (the finder, the diff and the probe) read meaning and new phrases. In the table above, only the deterministic checks caught three things: the 57-word sentence that a fluent reader repairs without noticing, the reason placed inside a rule that two reviewers had cleared, and the 29 semicolons in one letter. Only the model checks caught four: the clause that a weak reader took as a condition, the two-item list that a weak reader took as closed, the coined phrase that was not yet on any list, and the ordinary words that carried an insider meaning. | The flagged triad is "the finder, the diff and the probe," three distinct real checks named once, not padding, so per the rules it stays untouched and the paragraph is returned unchanged. · still flagged: TRIAD | The deterministic checks (Vale and the phrase lists) read form and known phrases. The model checks (the finder, the diff and the probe) read meaning and new phrases. In the table above, only the deterministic checks caught three things: the 57-word sentence that a fluent reader repairs without noticing, the reason placed inside a rule that two reviewers had cleared, and the 29 semicolons in one letter. Only the model checks caught four: the clause that a weak reader took as a condition, the two-item list that a weak reader took as closed, the coined phrase that was not yet on any list, and the ordinary words that carried an insider meaning. |
| 11 | TRIAD x1; LEADIN | The playbook below is self-contained. It carries the prerequisites, the Vale config, the three prompts, the calibration step, the loop and the costs. It also carries the five writing rules that make text pass, and the rewrites above. Hand it to an agent and say: set up the reader-hygiene lane from this playbook and run it on this file. You can also follow it by hand. | The playbook below is self-contained. It carries the prerequisites, the Vale config, the three prompts, the calibration step, the loop, the costs, the five writing rules that make text pass and the rewrites above. Hand it to an agent, tell it to set up the reader-hygiene lane from this playbook and run it on this file. You can also follow it by hand. | Merged the two parallel 'It carries...' sentences into one list, breaking the three-sentence triad rhythm, and replaced the 'say:' colon lead-in with plain 'tell it to' phrasing. · still flagged: TRIAD, KICKER, LEADIN | The playbook below is self-contained. It carries the prerequisites, the Vale config, the three prompts, the calibration step, the loop and the costs. It also carries the five writing rules that make text pass, and the rewrites above. Hand it to an agent and say: set up the reader-hygiene lane from this playbook and run it on this file. You can also follow it by hand. |
